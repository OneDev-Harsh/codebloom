{
  "name": "codebloom",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install && npm run build && mkdir -p dist/client && cp -r ../client/dist/* dist/client",
    "start": "node server/dist/server.js"
  }
}
